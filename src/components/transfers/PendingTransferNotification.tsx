// PendingTransferNotification is no longer needed since the transfer flow
// no longer requires receiver acceptance — transfers go directly to admin approval.
const PendingTransferNotification = () => null;
export default PendingTransferNotification;
