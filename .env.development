VITE_PROD_DOMAIN="myco-op.co.za"

# Local dev tenant resolution
# Use path-based tenant pages by default:
VITE_TENANT_ROUTING="path"

# Enable optional local subdomain tenancy (aem.localhost)
VITE_TENANT_DOMAIN="localhost"
VITE_DEV_TENANT_DOMAIN="localhost"
